# 纺织品后台管理系统

基于 Vue 3 + Vite + Element Plus 构建的现代化后台管理系统。

## 技术栈

- **前端框架**: Vue 3 (Composition API)
- **构建工具**: Vite 7
- **UI组件库**: Element Plus
- **状态管理**: Pinia
- **路由**: Vue Router 4
- **HTTP客户端**: Axios
- **图标**: @element-plus/icons-vue

## 项目结构

```
admin-panel/
├── src/
│   ├── api/              # API接口
│   │   └── admin.js      # 管理员接口
│   ├── assets/           # 静态资源
│   ├── components/       # 公共组件
│   ├── router/           # 路由配置
│   │   └── index.js      # 路由定义和守卫
│   ├── store/            # Pinia状态管理
│   │   └── user.js       # 用户状态
│   ├── utils/            # 工具函数
│   │   └── request.js    # Axios封装
│   ├── views/            # 页面组件
│   │   ├── Login.vue     # 登录页
│   │   ├── Layout.vue    # 主布局
│   │   └── Home.vue      # 首页Dashboard
│   ├── App.vue           # 应用根组件
│   └── main.js           # 应用入口
├── vite.config.js        # Vite配置
└── package.json
```

## 功能特性

### 已实现

✅ **用户认证**
- 登录/登出
- JWT Token管理
- 路由守卫
- Token自动刷新

✅ **布局框架**
- 响应式侧边栏（可折叠）
- 顶部导航栏
- 面包屑导航
- 用户信息展示

✅ **首页Dashboard**
- 数据统计卡片
- 快速操作入口
- 系统信息展示

### 开发中

🚧 **商品管理**
- 商品列表（查询、筛选、分页）
- 商品详情
- 添加/编辑商品
- 批量操作

🚧 **分类管理**
- 分类树形展示
- 添加/编辑/删除分类

🚧 **轮播图管理**
- 轮播图列表
- 添加/编辑轮播图
- 图片上传

🚧 **用户管理**
- 用户列表
- 用户详情
- 用户统计

🚧 **评论管理**
- 评论列表
- 评论审核
- 回复评论

🚧 **询价管理**
- 询价列表
- 处理询价
- 状态管理

🚧 **数据分析**
- 商品数据统计
- 用户行为分析
- 浏览时长分析
- 图表展示

## 开发指南

### 环境要求

- Node.js >= 18.20.8
- npm >= 10.8.2

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

访问地址: http://localhost:5173

### 构建生产版本

```bash
npm run build
```

### 预览生产版本

```bash
npm run preview
```

## 配置说明

### API代理配置

在 `vite.config.js` 中配置了开发服务器代理:

```javascript
server: {
  port: 5173,
  proxy: {
    '/api': {
      target: 'http://localhost:8080',
      changeOrigin: true
    }
  }
}
```

所有发送到 `/api` 的请求都会被代理到后端服务器 `http://localhost:8080`。

### 环境变量

创建 `.env.development` 和 `.env.production` 文件来配置不同环境的变量。

## API接口

### 后端API地址

- 开发环境: http://localhost:8080/api
- 生产环境: (待配置)

### 已接入的接口

| 接口 | 方法 | 说明 |
|------|------|------|
| /admin/login | POST | 管理员登录 |
| /admin/verify | GET | 验证Token |
| /admin/info | GET | 获取管理员信息 |
| /admin/logout | POST | 退出登录 |

### 请求格式

所有需要认证的请求都需要在Header中携带Token:

```
Authorization: Bearer <token>
```

### 响应格式

统一的响应格式:

```json
{
  "code": 200,
  "message": "成功",
  "data": {...}
}
```

## 默认账号

- **用户名**: admin
- **密码**: admin123

## 开发规范

### 命名规范

- **组件名**: PascalCase (如 `UserList.vue`)
- **文件名**: kebab-case 或 PascalCase
- **变量名**: camelCase
- **常量名**: UPPER_SNAKE_CASE

### Git提交规范

```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式调整
refactor: 重构
test: 测试相关
chore: 构建/工具相关
```

### 代码风格

- 使用 Composition API
- 优先使用 `<script setup>` 语法
- 使用 TypeScript (可选)
- 遵循 ESLint 规则

## 故障排除

### 1. 登录后立即退出

检查Token是否正确保存到localStorage:

```javascript
localStorage.getItem('admin_token')
```

### 2. API请求404

确认:
1. 后端服务已启动 (端口8080)
2. Vite代理配置正确
3. API路径包含 `/api` 前缀

### 3. 跨域问题

确认后端已配置CORS支持，或使用Vite代理。

## 路线图

### Phase 1 - 核心功能 (当前)
- ✅ 登录认证
- ✅ 基础布局
- 🚧 商品管理
- 🚧 分类管理
- 🚧 轮播图管理

### Phase 2 - 内容管理
- 用户管理
- 评论管理
- 询价管理

### Phase 3 - 数据分析
- 统计Dashboard
- 数据图表
- 报表导出

### Phase 4 - 系统优化
- 批量操作
- 权限管理
- 操作日志
- 系统设置

## 相关文档

- [Vue 3 文档](https://cn.vuejs.org/)
- [Vite 文档](https://cn.vitejs.dev/)
- [Element Plus 文档](https://element-plus.org/zh-CN/)
- [Pinia 文档](https://pinia.vuejs.org/zh/)
- [Vue Router 文档](https://router.vuejs.org/zh/)

## 许可证

MIT
