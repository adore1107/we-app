<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.songjia.textile.mapper.CategoryMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.songjia.textile.entity.Category">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="parent_id" property="parentId" jdbcType="INTEGER"/>
        <result column="level" property="level" jdbcType="INTEGER"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="icon" property="icon" jdbcType="VARCHAR"/>
        <result column="sort_order" property="sortOrder" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="BOOLEAN"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 基础查询列 -->
    <sql id="Base_Column_List">
        id, parent_id, level, name, icon, sort_order, status, created_at
    </sql>

    <!-- 根据状态查找分类 -->
    <select id="findByStatusOrderBySortOrderAsc" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM categories
        <if test="status != null">
            WHERE status = #{status}
        </if>
        ORDER BY sort_order ASC
    </select>

    <!-- 根据名称查找分类 -->
    <select id="findByNameAndStatus" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM categories
        WHERE name = #{name}
        <if test="status != null">
            AND status = #{status}
        </if>
        LIMIT 1
    </select>

    <!-- 根据ID查找分类 -->
    <select id="findById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM categories
        WHERE id = #{id}
    </select>

    <!-- 根据层级和状态查找分类 -->
    <select id="findByLevelAndStatusOrderBySortOrderAsc" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM categories
        WHERE level = #{level}
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY sort_order ASC
    </select>

    <!-- 根据父分类ID和状态查找分类 -->
    <select id="findByParentIdAndStatusOrderBySortOrderAsc" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM categories
        WHERE parent_id = #{parentId}
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY sort_order ASC
    </select>

</mapper>